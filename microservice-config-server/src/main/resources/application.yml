
server:
  port: 8888

logging:
  level:
    root: info

spring:
  application:
    name: microservice-config-server
  cloud:
    config:
      server:
        git:
          uri:
          username:
          password:
eureka:
  client:
    service-url:
      defaultZone: http://user:passwd@localhost:8761/eureka
  instance:
    instance-id: ${spring.application.name}-${server.port}
    prefer-ip-address: true
